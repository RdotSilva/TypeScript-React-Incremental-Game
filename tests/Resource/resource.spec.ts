import { test, expect } from "@playwright/test";

test.fixme("check default state", async ({ page }) => {});

test.fixme("check stat increase on click", async ({ page }) => {});

test.fixme("check upgrade buttons", async ({ page }) => {});
